import QtQuick
import Felgo

Item {

    id: cBIS

    /*
      The key of the map must be the start of the sub folder right after 'assets/'
      */
    property var mMAP_PATH_TO_PRIORITY: new Map([
                                            ["Images/librairies_escape/", 0],
                                            ["Images/librairies_basic/", 0]
                                            ]);


}
